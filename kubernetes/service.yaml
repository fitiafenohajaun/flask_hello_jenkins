apiVersion: v1
kind: Service

metadata:
  name: pythontest
  labels:
    app: pythontest

spec:
  type: NodePort

  selector:
    app: pythontest

  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 31000